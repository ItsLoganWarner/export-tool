export default {
    fields: {
      modelName: {
        type: 'string',
        default: '',
        tip: 'Vehicle model name',
        // JSON path into the parsed object:
        path: ['Name']
      }
    }
  };
  